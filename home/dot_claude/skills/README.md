Fleet-wide Claude Code skills. Each subfolder is a skill (`SKILL.md` + files), synced to every laptop by chezmoi.
Start by copying the ones you use on the Mac from `~/.claude/skills/` into this folder and committing.
