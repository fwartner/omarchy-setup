# Codex CLI. Login is interactive (`codex login`), not from the vault.
model = "gpt-5-codex"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[mcp_servers.context7]
url = "https://mcp.context7.com/mcp"
