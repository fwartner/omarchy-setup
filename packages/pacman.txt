# Arch / Omarchy repo packages (installed via omarchy-pkg-add). One per line.
# Omarchy's own install/omarchy-base.packages (150 entries) is installed on every
# machine, so nothing from it is repeated here. Checked with:
#   comm -12 <(./scripts/pkglist.sh pacman|sort -u) <(omarchy base+other packages)
# --- shell & terminal
chezmoi
rbw
pinentry
ghostty
atuin
yazi
television
gitui
git-delta
difftastic
go-yq
fx
htop
ncdu
tree
zip
rsync
wget
curl
openssh
just
hyperfine
tokei
dust
duf
procs
sd
typos
tealdeer
glow
zk
# --- git workflow
github-cli
jujutsu
pre-commit
gitleaks
git-absorb
git-branchless
# --- dev
podman
podman-compose
# podman-docker is deliberately NOT here: it declares `conflicts: [docker]`, and
# omarchy-base installs `docker` on every machine, so pacman would refuse the
# transaction mid-bootstrap. 50-containers.sh aliases `docker` to podman instead,
# which gets the same muscle memory with no package conflict.
buildah
mise
shellcheck
xh
pgcli
valkey
atac
# --- kubernetes / infra (clients)
kubectl
helm
k9s
kubectx
stern
argocd
velero
krew
hcloud
opentofu
terraform
aws-cli-v2
logcli
lsof
# --- mesh, sync, backup
tailscale
syncthing
restic
# --- hardware / laptop
fwupd
# --- network diagnostics
gping
trippy
bandwhich
dog
# --- gui
bitwarden
telegram-desktop
dbeaver
